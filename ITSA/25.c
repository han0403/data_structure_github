#include <stdio.h>

int main()
{
    float total_quantity, per_quantity;             //¨T¨®¯Óªo
    scanf("%f%f", &total_quantity, &per_quantity);
    printf("%.1f\n", total_quantity/per_quantity/60);

    return 0;
}
